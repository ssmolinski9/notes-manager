/**
 * Category module provides screen of specified category
 * with notes list and allows works with notes (adding, deleting, updating).
 * Module is connected with database and changes are automatically save.
 */
package category;