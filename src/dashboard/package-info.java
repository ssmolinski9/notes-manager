/**
 * Dashboard module provides application first screen
 * with categories list and allows works with categories (adding, deleting, updating).
 * Module is connected with database and changes are automatically save.
 */
package dashboard;